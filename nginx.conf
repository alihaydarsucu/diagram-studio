server { 
 listen 8080;
 server_name mermaid;
 location / {
   root /usr/share/nginx/html;
	 auth_basic "Diagram Studio";
	 auth_basic_user_file /etc/nginx/.htpasswd;
	 # Fallback to index.html for other paths
   try_files $uri /index.html;
 }
}
